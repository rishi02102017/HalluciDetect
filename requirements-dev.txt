# Development dependencies (includes heavy ML models)
# Install with: pip install -r requirements-dev.txt

# Include all production dependencies
-r requirements.txt

# Heavy ML dependencies (optional, for better semantic similarity)
# Only used when USE_LOCAL_EMBEDDINGS=true
sentence-transformers>=2.3.0

# Development tools
pytest>=7.4.0
black>=24.0.0
flake8>=7.0.0

